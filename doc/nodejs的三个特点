一、nodejs的三个特点：
1、单线程：单线程即假如有多个请求，也只开一个线程，让程序代码一句一句地按顺序执行下去
2、非阻塞I/O：当执行到有I/O操作的语句时，CPU不等待，而是跳过直接执行下一句
3、事件驱动：刚才跳过的I/O操作取到数据后，借助回调函数返回，再借助事件驱动根据一定的优先级打入到事件环

二、而像java这种就是阻塞I/O，所以要想提高CPU利用率，则利用的是多线程并发形式

三、nodejs适合用于处理I/O密集（即I/O操作、数据处理多）的后台程序，
而java适合处理CPU密集（逻辑计算多）的后台程序